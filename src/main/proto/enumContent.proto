syntax = "proto3";

option java_package = "com.digisky.liuwei2.protobuf";

enum Corpus {
    UNIVERSAL = 0;
    WEB = 1;
    IMAGES = 2;
    LOCAL = 3;
    NEWS = 4;
    PRODUCTS = 5;
    VIDEO = 6;
}

// 允许枚举字段别名
enum EnumAllowingAlias {
    option allow_alias = true;  // 字段别名允许名称
    UNKNOWN = 0;
    STARTED = 1;
    RUNNING = 1;
}